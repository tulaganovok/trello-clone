import { Button } from "#/components/ui/button";

export default function Pricing() {
  return (
    <Button
      variant="ghost"
      className="h-full text-base font-normal hover:text-primary hover:bg-transparent group"
    >
      Pricing
    </Button>
  )
}
